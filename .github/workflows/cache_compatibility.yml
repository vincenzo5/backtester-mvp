name: Cache Compatibility Tests

on:
  pull_request:
    paths:
      - 'src/backtester/data/cache_manager.py'
      - 'src/backtester/data/validator.py'
      - 'src/backtester/data/updater.py'
      - 'src/backtester/data/fetcher.py'
  push:
    branches: [main]
    paths:
      - 'src/backtester/data/cache_manager.py'
      - 'src/backtester/data/validator.py'
      - 'src/backtester/data/updater.py'
      - 'src/backtester/data/fetcher.py'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Run compatibility tests
      run: |
        pytest tests/test_cache_compatibility.py -v

